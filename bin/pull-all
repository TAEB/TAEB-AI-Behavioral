#!/usr/bin/env perl
@ARGV = "REPOSITORIES";
@ARGV = "etc/REPOSITORIES" if -e "etc/REPOSITORIES";

while (<>) {
    next unless /^\* (.*)/;
    print "Pulling from $1...\n";
    system("darcs pull $1");
}

